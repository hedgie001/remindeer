"main";let express;_54d‍.w('express',[["default",function(v){express=v}]]);let bodyParser;_54d‍.w('body-parser',[["default",function(v){bodyParser=v}]]);let path;_54d‍.w('path',[["default",function(v){path=v}]]);let orderRoutes;_54d‍.w('./routes/orderRoutes',[["orderRoutes",function(v){orderRoutes=v}]]);





const app = express();

const router = express.Router();

app.use(express.static(path.resolve('public')));

app.use(bodyParser.json());

app.get("/", function(req, res){
    res.sendFile("/index.html",  {root: __dirname + '/public/'});
});
app.use("/", orderRoutes);

const hostname = '127.0.0.1';
const port = 3001;
app.listen(port, hostname, () => {
    console.log(`Server running at http://${hostname}:${port}/`);
});